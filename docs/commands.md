[← Documentation index](./index.md)

# Command reference

<!-- Generated by scripts/generate-command-docs.js from package.json. Do not edit by hand. -->

Every command is available from the Command Palette under the
**TIFF Visualizer** category. Many are also in the right-click menu inside an
open image.

## Opening and navigation

| Command | ID | Keybinding |
| --- | --- | --- |
| Open Folder as DICOM Dataset | `tiffVisualizer.openDicomFolder` |  |
| Zoom In | `tiffVisualizer.zoomIn` |  |
| Zoom Out | `tiffVisualizer.zoomOut` |  |
| Reset Zoom | `tiffVisualizer.resetZoom` |  |
| Paste Position | `tiffVisualizer.pastePosition` |  |

See [getting-started](./getting-started.md).

## Display settings

| Command | ID | Keybinding |
| --- | --- | --- |
| Set Normalization Range | `tiffVisualizer.setNormalizationRange` |  |
| Set Gamma | `tiffVisualizer.setGamma` |  |
| Set Brightness | `tiffVisualizer.setBrightness` |  |
| Toggle NaN Color (Black/Fuchsia) | `tiffVisualizer.toggleNanColor` |  |
| Toggle RGB as 24-bit Grayscale | `tiffVisualizer.toggleRgb24Mode` |  |
| Apply Colormap (Pseudocolor) | `tiffVisualizer.applyColormap` |  |
| Decode Colormap Image to Float | `tiffVisualizer.convertColormapToFloat` |  |
| Revert to Original Image | `tiffVisualizer.revertToOriginal` |  |
| Reset All Settings to Defaults | `tiffVisualizer.resetAllSettings` |  |

See [rendering](./rendering.md).

## Inspection panels

| Command | ID | Keybinding |
| --- | --- | --- |
| Toggle Histogram | `tiffVisualizer.toggleHistogram` | `ctrl` + `h` / `cmd` + `h (macOS)` |
| Toggle Metadata Panel | `tiffVisualizer.toggleMetadata` | `ctrl` + `m` / `cmd` + `m (macOS)` |
| Measure | `tiffVisualizer.toggleMeasure` | `ctrl` + `shift` + `m` / `cmd` + `shift` + `m (macOS)` |
| Toggle Color Picker: Show Modified Values | `tiffVisualizer.toggleColorPickerMode` |  |
| Debayer (Demosaic CFA Image) | `tiffVisualizer.toggleDebayer` |  |

See [viewing](./viewing.md).

## Collections and comparison

| Command | ID | Keybinding |
| --- | --- | --- |
| Add Images to Collection | `tiffVisualizer.browseAndAddToCollection` |  |
| Select Image for Side-by-Side Compare | `tiffVisualizer.selectForCompare` |  |
| Compare Side by Side with Selected | `tiffVisualizer.compareWithSelected` |  |
| Open Comparison Gallery (Legacy Renderer) | `tiffVisualizer.openComparisonPanel` |  |

See [collections](./collections.md).

## Layers

| Command | ID | Keybinding |
| --- | --- | --- |
| Open Layers View | `tiffVisualizer.toggleLayers` |  |
| Add Image as Layer | `tiffVisualizer.addLayer` |  |

See [layers](./layers.md).

## Export and interchange

| Command | ID | Keybinding |
| --- | --- | --- |
| Export… | `tiffVisualizer.exportLayers` |  |
| Copy Image | `tiffVisualizer.copyImage` |  |
| Copy Image Information to Clipboard | `tiffVisualizer.copyImageInfo` | `ctrl` + `shift` + `i` / `cmd` + `shift` + `i (macOS)` |
| Open as Point Cloud | `tiffVisualizer.openAsPointCloud` |  |

See [export](./export.md).

## Help

| Command | ID | Keybinding |
| --- | --- | --- |
| Show Documentation | `tiffVisualizer.showDocumentation` |  |

See [index](./index.md).

## Other

| Command | ID | Keybinding |
| --- | --- | --- |
| Open DICOM Volume in 3D Viewer | `tiffVisualizer.openVolumeIn3D` |  |
| Channels | `tiffVisualizer.toggleChannels` |  |
| Toggle Scale Bar | `tiffVisualizer.toggleScaleBar` |  |

## Keybindings

These are active only while a Scientific Image Visualizer tab has focus, so
they do not shadow your normal editor bindings.

| Keybinding | Command |
| --- | --- |
| `ctrl` + `h` / `cmd` + `h (macOS)` | Toggle Histogram |
| `ctrl` + `m` / `cmd` + `m (macOS)` | Toggle Metadata Panel |
| `ctrl` + `shift` + `m` / `cmd` + `shift` + `m (macOS)` | Measure |
| `ctrl` + `shift` + `i` / `cmd` + `shift` + `i (macOS)` | Copy Image Information to Clipboard |

Inside the image, `←` / `→` step through a dataset, collection or multi-page
TIFF, and `[` / `]` step through TIFF pages. Inside the measure panel, single
letters select tools — see [measurement](./measure.md).

## Settings

| Setting | Default | Description |
| --- | --- | --- |
| `tiffVisualizer.gpuAcceleration` | `true` | Use GPU acceleration via WebGPU or WebGL2 for supported image rendering paths. Disable this if a GPU/driver causes incorrect output or slower rendering. |

Display settings — normalization, gamma, brightness — are deliberately *not*
VS Code settings. They live for the lifetime of the window and reset when it
closes. See [rendering](./rendering.md).

## File associations

| Editor | Opens |
| --- | --- |
| Scientific Image Visualizer (`tiffVisualizer.previewEditor`) | `*.{tif,tiff,tf2,tf8,btf,exr,pfm,npy,npz,ppm,pgm,pbm,hdr,tga,jxl,fits,fit,fts,dcm,dicom,DCM,DICOM,nc,cdf,czi,nd2,lif,ora,kra,psd,psb,xcf,afphoto,af}` |
| Scientific Image Visualizer (`tiffVisualizer.previewEditor.option`) | `*.{png,jpeg,jpg,bmp,ico,webp,avif}` |

The `.option` editor is registered for formats VS Code already previews, so
it does not take them over — reach it with **Open With…**. See
[formats](./formats.md).
